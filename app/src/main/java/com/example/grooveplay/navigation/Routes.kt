package com.example.grooveplay.navigation

/**
 * Central place for every navigable route in the app. Screens reference
 * these constants instead of hardcoding route strings, so a renamed
 * route only breaks in one place.
 */

object Routes {
    const val HOME = "home"
    const val ONBOARDING = "onboarding"
}