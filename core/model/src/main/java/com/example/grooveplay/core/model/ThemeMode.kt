package com.example.grooveplay.core.model

/**
 * The three theme options supported by the application.
 * SYSTEM follows the device setting; LIGHT/DARK override it explicitly.
 */
enum class ThemeMode {
    SYSTEM, LIGHT, DARK
}
